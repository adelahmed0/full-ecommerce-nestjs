# تعليمات مشروع Full E-Commerce (NestJS)

مشروع **Backend فقط** (NestJS API). الفريق **3 AI Agents** في `.cursor/agents/`.

## قاعدة ذهبية — استلام التاسكات

1. **أي تاسك من المستخدم يستلمها عادل أولاً** (`/adel`) — تخطيط وتوزيع فقط.
2. بعد خطة عادل، **محمود** (`/mahmoud`) ينفّذ الكود.
3. بعد التنفيذ، **فاطمة** (`/fatima`) تختبر وتغلق القبول.
4. **ممنوع** أن يبدأ أي تنفيذ مباشرة بدون مرور على عادل.

مثال صحيح من المستخدم:
```text
/adel عايز CRUD للـ Category
```

## برانش الشغل

- برانش العمل الأساسي: **`cursor/backend-dev-475f`**
- كل الشغل يتم على هذا البرانش أو من فروع متفرعة منه
- **لا ترفع ولا تدمج مباشرة على `master`** إلا بطلب صريح

| الاستدعاء | الاسم | الدور |
|-----------|------|--------|
| `/adel` | عادل | مدير المشروع — يستلم التاسك ويوزّع |
| `/mahmoud` | محمود | Backend — ينفّذ بعد خطة عادل |
| `/fatima` | فاطمة | QA — تختبر بعد تنفيذ محمود |

## سير العمل

1. المستخدم → `/adel`
2. عادل يكتب الخطة ويعين محمود / فاطمة
3. `/mahmoud` ينفّذ على فرع من `cursor/backend-dev-475f`
4. `/fatima` تتحقق قبل الإغلاق

## قواعد تقنية

- NestJS + TypeScript + Mongoose/MongoDB فقط — لا Frontend
- اتبع أنماط `src/` الحالية
- المتطلبات: `docs/1-Requirements-ex.docx`
- تفاصيل الفريق: `docs/TEAM.md`
